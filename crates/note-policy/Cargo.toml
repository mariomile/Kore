[package]
name = "reflect-note-policy"
version = "0.1.0"
description = "Shared frontmatter and title-derivation policy for Reflect's native surfaces"
edition = "2021"

[dependencies]
saphyr = "0.0.6"
pulldown-cmark = "0.13.4"
